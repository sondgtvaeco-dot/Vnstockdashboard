# Copy file này thành .streamlit/secrets.toml (đã bị .gitignore, không commit lên Git)
# rồi điền giá trị thật. Khi deploy lên Streamlit Cloud, dán cùng nội dung này
# (với giá trị thật) vào Settings → Secrets trên trang quản trị app.

APP_PASSWORD = "doi-mat-khau-nay"

DB_URL = "postgresql://postgres:<password>@<host>:5432/postgres"
